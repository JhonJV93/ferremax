import { defineConfig } from '@prisma/client'
import { PrismaPostgresAdapter } from '@prisma/adapter-postgres'
import { Pool } from 'pg'

const pool = new Pool({
  connectionString: process.env.DATABASE_URL
})

export default defineConfig({
  adapter: new PrismaPostgresAdapter(pool)
})